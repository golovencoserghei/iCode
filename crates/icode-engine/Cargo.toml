[package]
name = "icode-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "iCode v2 engine: parse / index / store / memory / search (impl from M1)"

[dependencies]
icode-core = { workspace = true }
anyhow = { workspace = true }
rusqlite = { workspace = true }
sqlite-vec = { workspace = true }
tree-sitter = { workspace = true }
tree-sitter-rust = { workspace = true }
tree-sitter-python = { workspace = true }
tree-sitter-php = { workspace = true }
walkdir = { workspace = true }
sha2 = { workspace = true }
serde_json = { workspace = true }
regex = { workspace = true }

[dev-dependencies]
tempfile = "3"
# DEV-ONLY: the live embed_e2e test needs a concrete Embedder. icode-engine's
# production graph stays decoupled from icode-embed — it depends only on the
# `Embedder` trait from icode-core (see embed_pending). This dep is NOT in
# [dependencies].
icode-embed = { workspace = true }
