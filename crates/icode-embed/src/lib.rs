//! `icode-embed` — concrete `Embedder` implementations.
//!
//! M2: `OllamaEmbedder` (default, validated against qwen3-embedding:0.6b → dim
//! 1024) and `FastEmbedder` (ONNX, offline fallback). Both implement
//! `icode_core::traits::Embedder`.

// pub mod ollama;     // M2
// pub mod fastembed;  // M2
